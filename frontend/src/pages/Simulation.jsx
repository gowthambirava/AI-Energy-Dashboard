import { useState } from 'react'; import api from '../api/axios'; import Layout from '../components/Layout'; import StatCard from '../components/StatCard'; import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid } from 'recharts';
export default function Simulation(){ const [form,setForm]=useState({scenario:'peak_load_reduction',percent:10,cost_per_kwh:8}); const [result,setResult]=useState(null); const [msg,setMsg]=useState(''); const run=async()=>{setMsg('Running scenario simulation...'); try{const {data}=await api.post('/energy/simulate',{...form, forecast_id: Number(localStorage.getItem('lastForecastId'))||null}); setResult(data); setMsg('Simulation completed')}catch(e){setMsg(e.response?.data?.detail||'Simulation failed')}}; return <Layout><h1 className="text-5xl font-black mb-2">Scenario Simulation</h1><p className="text-yellow-100 mb-6">Estimate savings and cost impact for occupancy changes, temperature changes, shutdowns and peak-hour reductions.</p><div className="glass rounded-[2rem] p-6 grid md:grid-cols-4 gap-4 mb-6"><select className="input" value={form.scenario} onChange={e=>setForm({...form,scenario:e.target.value})}><option value="peak_load_reduction">Peak-hour load reduction</option><option value="device_shutdown">Device shutdown</option><option value="increased_occupancy">Increased occupancy</option><option value="temperature_increase">Temperature increase</option></select><input className="input" type="number" value={form.percent} onChange={e=>setForm({...form,percent:Number(e.target.value)})} placeholder="Percent"/><input className="input" type="number" value={form.cost_per_kwh} onChange={e=>setForm({...form,cost_per_kwh:Number(e.target.value)})} placeholder="Cost/kWh"/><button className="btn-primary" onClick={run}>Run Simulation</button><p className="md:col-span-4 text-yellow-200">{msg}</p></div>{result&&<><div className="grid md:grid-cols-4 gap-5 mb-6"><StatCard title="Baseline" value={result.baseline_kwh+' kWh'} icon="📊"/><StatCard title="Simulated" value={result.simulated_kwh+' kWh'} icon="🧪" color="from-blue-400 to-cyan-500"/><StatCard title="Savings" value={result.savings_kwh+' kWh'} icon="💡" color="from-yellow-300 to-red-400"/><StatCard title="Cost Impact" value={'₹'+result.cost_savings} icon="💰" color="from-green-400 to-blue-500"/></div><section className="glass rounded-[2rem] p-6"><h2 className="text-2xl font-black mb-4">Baseline vs Simulated</h2><ResponsiveContainer width="100%" height={360}><LineChart data={result.series}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="timestamp" hide/><YAxis/><Tooltip/><Line dataKey="baseline_kwh" stroke="#facc15" strokeWidth={3}/><Line dataKey="simulated_kwh" stroke="#60a5fa" strokeWidth={3}/></LineChart></ResponsiveContainer></section></>}</Layout> }
