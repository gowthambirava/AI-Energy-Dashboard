import { useEffect, useState } from 'react'; import api from '../api/axios'; import Layout from '../components/Layout'; import StatCard from '../components/StatCard'; import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, BarChart, Bar, CartesianGrid, LineChart, Line } from 'recharts';
export default function Dashboard(){ const [data,setData]=useState(null); useEffect(()=>{api.get('/energy/overview').then(r=>setData(r.data)).catch(()=>{})},[]); const latest=data?.latest||{}; const predictions=latest?.predictions||[]; const historical=latest?.historical||[]; const peaks=latest?.peaks||[]; return <Layout><div className="flex flex-col lg:flex-row justify-between gap-4 mb-8"><div><h1 className="text-5xl font-black">AI Energy Dashboard</h1><p className="text-yellow-100">Live-style overview for energy usage, peak load, anomalies and cost-saving opportunities.</p></div><div className="glass rounded-3xl px-5 py-4"><p className="text-sm text-white/70">System Status</p><p className="font-black text-yellow-200">● Monitoring Active</p></div></div><div className="grid md:grid-cols-4 gap-5 mb-8"><StatCard title="Total Energy" value={(data?.total_kwh||0)+' kWh'} icon="⚡"/><StatCard title="Forecast Accuracy" value={(data?.accuracy||0)+'%'} icon="🎯" color="from-blue-400 to-cyan-500"/><StatCard title="Peak Alerts" value={peaks.length} icon="🚨" color="from-red-500 to-yellow-400"/><StatCard title="Datasets" value={data?.datasets_count||0} icon="📁" color="from-indigo-500 to-blue-600"/></div><div className="grid xl:grid-cols-2 gap-6"><section className="glass rounded-[2rem] p-6"><h2 className="text-2xl font-black mb-4">Historical vs Predicted Usage</h2><ResponsiveContainer width="100%" height={330}><AreaChart data={[...historical.map(x=>({time:x.timestamp, actual:x.actual_kwh})), ...predictions.map(x=>({time:x.timestamp, predicted:x.predicted_kwh}))]}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="time" hide/><YAxis/><Tooltip/><Area dataKey="actual" stroke="#fde047" fill="#fde047" fillOpacity={0.35}/><Area dataKey="predicted" stroke="#60a5fa" fill="#60a5fa" fillOpacity={0.35}/></AreaChart></ResponsiveContainer></section><section className="glass rounded-[2rem] p-6"><h2 className="text-2xl font-black mb-4">Hourly Energy Usage</h2><ResponsiveContainer width="100%" height={330}><BarChart data={data?.hourly_usage||[]}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="hour"/><YAxis/><Tooltip/><Bar dataKey="energy_kwh" fill="#facc15"/><Bar dataKey="usage" fill="#facc15"/><Bar dataKey="consumption" fill="#facc15"/></BarChart></ResponsiveContainer></section><section className="glass rounded-[2rem] p-6"><h2 className="text-2xl font-black mb-4">Device / Building Analytics</h2><ResponsiveContainer width="100%" height={300}><BarChart data={data?.device_usage||[]}><XAxis dataKey="device"/><YAxis/><Tooltip/><Bar dataKey="kwh" fill="#ef4444"/></BarChart></ResponsiveContainer></section><section className="glass rounded-[2rem] p-6"><h2 className="text-2xl font-black mb-4">Peak Usage Alerts</h2><div className="space-y-3 max-h-72 overflow-auto">{peaks.length?peaks.map((p,i)=><div key={i} className="bg-red-500/30 border border-red-300/30 rounded-2xl p-4"><b>{p.predicted_kwh} kWh</b><p className="text-sm">{p.alert}</p></div>):<p className="text-yellow-100">Train a forecast to generate peak alerts.</p>}</div></section></div></Layout> }
