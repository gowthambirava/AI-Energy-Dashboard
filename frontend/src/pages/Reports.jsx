import { useEffect, useState } from 'react'; import api from '../api/axios'; import Layout from '../components/Layout';
export default function Reports(){ const [rows,setRows]=useState([]); useEffect(()=>{api.get('/forecast/history').then(r=>setRows(r.data))},[]); const download=(id,fmt)=>{window.open(`http://127.0.0.1:8000/api/reports/${id}/download?format=${fmt}`,'_blank')}; return <Layout><h1 className="text-5xl font-black mb-2">Export Reports</h1><p className="text-yellow-100 mb-6">Download forecast results as Excel or PDF for submission or review.</p><div className="glass rounded-[2rem] p-6 overflow-auto"><table className="w-full"><thead><tr className="text-left bg-blue-500/30"><th className="p-3">ID</th><th>Dataset</th><th>Accuracy</th><th>Periods</th><th>Download</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td className="table-cell">#{r.id}</td><td className="table-cell">{r.dataset_id}</td><td className="table-cell">{r.accuracy}%</td><td className="table-cell">{r.periods}</td><td className="table-cell"><button onClick={()=>download(r.id,'excel')} className="bg-yellow-300 text-blue-950 px-4 py-2 rounded-xl mr-2 font-bold">Excel</button><button onClick={()=>download(r.id,'pdf')} className="bg-red-500 text-white px-4 py-2 rounded-xl font-bold">PDF</button></td></tr>)}</tbody></table></div></Layout> }
