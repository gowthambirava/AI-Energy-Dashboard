import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import api from '../api/axios'; import { useAuth } from '../context/AuthContext';
export default function Login(){ const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [err,setErr]=useState(''); const {login}=useAuth(); const nav=useNavigate();
 const submit=async(e)=>{e.preventDefault(); setErr(''); try{const body=new URLSearchParams(); body.append('username',email); body.append('password',password); const {data}=await api.post('/auth/login',body); login(data.access_token,data.user); nav('/dashboard')}catch{setErr('Invalid login details')}};
 return <div className="min-h-screen grid place-items-center bg-[radial-gradient(circle_at_top,#facc15,#ef4444_38%,#1d4ed8_82%)] p-4"><form onSubmit={submit} className="glass-dark w-full max-w-md rounded-[2rem] p-8 shadow-2xl border border-yellow-300/40"><h1 className="text-4xl font-black text-white">EnergyAI Login</h1><p className="text-yellow-100 mb-6">Real-time energy forecasting & optimization</p>{err&&<p className="bg-red-500/80 p-3 rounded-xl text-white mb-3">{err}</p>}<input className="input mb-3" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input mb-5" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="w-full btn-primary">Login</button><p className="text-white mt-5">New user? <Link className="font-bold underline text-yellow-200" to="/register">Create account</Link></p></form></div> }
